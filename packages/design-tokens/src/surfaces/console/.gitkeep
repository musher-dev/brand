# The "console" surface is the canonical baseline — it has no overrides.
# Its build is simply primitives + base semantics. This file keeps the
# directory under version control as documentation of that intent.
