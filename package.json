{
  "name": "@musher-dev/brand",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "check": "bun run --filter '*' check",
    "lint": "bun run --filter '*' lint",
    "publint": "bun run --filter '*' publint",
    "clean": "bun run --filter '*' clean",
    "dev:ui": "bun run --filter '@musher-dev/design-tokens' build && bun run --filter '@musher-dev/svelte-ui' dev",
    "storybook:build": "bun run --filter '@musher-dev/design-tokens' build && bun run --filter '@musher-dev/svelte-ui' build-storybook"
  },
  "devDependencies": {
    "publint": "^0.3.0"
  }
}
